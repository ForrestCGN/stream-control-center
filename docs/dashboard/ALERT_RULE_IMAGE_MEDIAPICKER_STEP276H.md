# Alert Rule Image MediaPicker STEP276H

Im Alert-Regel-Editor gibt es jetzt eine Auswahl für `Regel-Grafik aus Media-Registry`.

Die alte Grafik-Auswahl bleibt als `Alte Grafik / Fallback` erhalten. Die Darstellung ist funktional; ein endgültiger UI-Feinschliff ist für das spätere Dashboard-Redesign vorgesehen.
