# VIP Preview Stable Check – STEP404B

STEP404B ersetzt den fehlerhaften STEP404A-Check, der eine nicht vorhandene Route `/api/communication/event` aufgerufen hat.

Der VIP-Preview-Flow selbst bleibt unverändert:

```text
vip.overlay.show -> VIP-Overlay sichtbar
vip.overlay.hide -> VIP-Overlay ausgeblendet
Sound-System bleibt unberührt
```
