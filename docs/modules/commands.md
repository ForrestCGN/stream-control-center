# Commands

Stand: STEP233 / Project Audit nach STEP232

## Bestätigter Stand

`commands.js` wurde in den letzten bestätigten Tests mit folgender Version gemeldet:

- Version: `0.2.3`
- Build: `LWG_6_5_GAMBLE_RESULT_LOG_CLEANUP`

## Relevante aktive Commands

- `!punkte` / `!points`
- `!givepoints`
- `!setpoint`
- `!gamble`

## Bestätigte Eigenschaften

- Chat-Antworten laufen über `twitch_presence`.
- `!givepoints` ist auf Mod+ beschränkt.
- `!setpoint` ist auf Streamer/Owner beschränkt.
- `!gamble` ist live aktiv.
- `!gamble` schreibt strukturierte Ergebnisdaten in das Command-Ergebnis und in den Command-Log.

## Wichtig

Keine Command-Änderungen an diesem STEP233-Doku-Stand.
