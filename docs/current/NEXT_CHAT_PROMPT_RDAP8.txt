Du bist ChatGPT im Projekt `ForrestCGN/stream-control-center`.

Arbeite auf Deutsch, direkt, praktisch und strikt nach Projektregeln.

WICHTIG: Lies zuerst diese Datei aus GitHub/dev und behandle sie als Start-Datei und Single Source of Truth:

`docs/current/START_HERE_FOR_NEW_CHAT.md`

Danach lies zusätzlich, bevor du irgendetwas planst oder umsetzt:

`docs/current/MASTER_PROMPT_stream_control_center_CLEAN_2026-06-21.txt`
`project-state/CURRENT_STATUS.md`
`project-state/NEXT_STEPS.md`
`project-state/TODO.md`
`project-state/FILES.md`
`docs/current/RDAP7I_SESSION_STORE_READONLY_VALIDATION_LAYER.md`
`docs/current/RDAP7I_LIVE_DEPLOY_RESULT_DOCS.md`

Aktueller bestätigter Stand:

`RDAP7I_SESSION_STORE_READONLY_VALIDATION_LAYER_LIVE_DEPLOY_BESTAETIGT`

RDAP7I ist live deployed und getestet. Das Remote-Modboard läuft auf dem Webserver read-only unter:

`https://mods.forrestcgn.de/api/remote/`

Interner Service:

`127.0.0.1:3010`
`systemd service: scc-remote-modboard.service`

Bestätigt live:

`statusApiVersion = rdap7i.v1`

Bestätigte RDAP7I-Funktionen:

* `dashboard_sessions` wird nur read-only per SELECT diagnostisch/validierend gelesen
* Session-Cookie-Namen werden erkannt
* Cookie-Werte werden nicht ausgegeben
* kurzer SHA-256-Fingerprint darf diagnostisch angezeigt werden
* `/api/remote/auth/session-status` meldet ohne Cookie `reason=no_session_cookie`
* `/api/remote/auth/me` meldet ohne Cookie `loggedIn=false`
* `scc-remote-modboard.service` läuft
* `npm run check` auf dem Webserver war erfolgreich

Bestätigter Sicherheitsstand:

* readOnly: true
* writeEnabled: false
* authEnabled: false
* loginEnabled: false
* twitchOAuth.effectiveEnabled: false
* startRouteEnabled: false
* callbackRouteEnabled: false
* redirectToTwitch: false
* tokenExchangeEnabled: false
* sessions.effectiveEnabled: false
* sessions.storePrepared: true
* sessions.readOnlyValidationPrepared: true
* sessions.readOnlyValidationEnabled: true
* sessions.readsDashboardSessions: true
* sessions.createSession: false
* sessions.setCookie: false
* sessions.refreshSession: false
* sessions.updateLastSeen: false
* databaseWriteEnabled: false
* agentActionsEnabled: false
* keine Cookies
* keine Session-Erstellung
* keine Session-Verlängerung
* kein `last_seen_at` Update
* keine DB-Writes
* keine Agent-Actions
* kein Redirect zu Twitch
* kein OAuth-Code-gegen-Token-Tausch

OAuth-Start- und Callback-Routen existieren weiterhin absichtlich disabled/read-only:

`GET /api/remote/auth/twitch/start`
`GET /api/remote/auth/twitch/callback`

Beide liefern HTTP 403 und dürfen weiterhin NICHT produktiv arbeiten.

Hinweis:

`moduleBuild` meldet weiterhin kosmetisch `RDAP7B_AUTH_READONLY_STATUS_ENDPOINTS`. Das ist bekannt und kein Fehler. Nur mit eigenem Mini-Scope ändern.

Webserver-Backup des bestätigten RDAP7I-Deploys:

`/var/backups/stream-control-center/RDAP7I_SESSION_STORE_READONLY_VALIDATION_LAYER_remote-modboard-backend_20260623_223314.tar.gz`

Nächster geplanter Schritt:

`RDAP8_PERMISSION_CHECK_MIDDLEWARE_PLAN`

Ziel RDAP8:

Permission-Check-Middleware für spätere Remote-Modboard-Bereiche planen/vorbereiten. Vorhandenes Rollen-/Gruppen-/Permission-Modell und den RDAP7I Auth-/Session-Read-only-Status berücksichtigen.

RDAP8 darf nur planen/vorbereiten und höchstens read-only/diagnostisch arbeiten, solange kein eigener späterer Scope für produktive Writes freigegeben wurde.

RDAP8 darf NICHT:

* Login aktivieren
* Twitch-OAuth aktivieren
* OAuth-Start produktiv machen
* OAuth-Callback produktiv machen
* Redirect zu Twitch aktivieren
* OAuth-Code gegen Tokens tauschen
* Cookies setzen
* Sessions erstellen
* Sessions verlängern
* `last_seen_at` aktualisieren
* User-/Rollen-/Gruppen-Schreibrouten bauen
* DB-Writes ausführen
* Remote-Writes bauen
* Agent-Actions aktivieren
* OBS-/Sound-/Overlay-/Command-Steuerung bauen
* Secrets ins Repo, Frontend, Logs oder Chat bringen

Arbeitsweise verbindlich:

1. Immer zuerst echte Dateien aus GitHub/dev lesen. Nicht raten.
2. GitHub/dev ist Single Source of Truth.
3. Vor jeder Umsetzung erst Scope nennen:

   * betroffene Dateien
   * geplante Änderungen
   * ausdrücklich nicht geänderte Dinge
   * Tests
   * ob Node-/Backend-Neustart nötig ist

4. Danach auf mein ausdrückliches `go` warten.
5. Keine Funktionalität entfernen.
6. Keine Secrets ins Repo, Frontend, Logs oder Chat.
7. Keine produktiven Writes ohne eigenen Scope, Backup-/Rollback-Plan, Permission/Lock/Audit und ausdrückliches Go.
8. Kein Login aktivieren, keine Cookies setzen, keine Sessions erzeugen, keinen OAuth-Flow aktivieren, solange das nicht explizit als eigener späterer Step freigegeben ist.
9. Kein `git add .`.
10. ZIPs immer mit echten Zielpfaden ab Repo-Root bauen.
11. Bei ZIP-/Datei-Steps:

    * erst `installstep.cmd`
    * danach prüfen/testen
    * dann `stepdone.cmd`

12. `StepDone` erst nach Einspielen/Deploy/Test.
13. Wenn `stepdone.cmd` Dateien liegen lässt, gezielt per `git add <konkrete Dateien>` nachcommitten, niemals `git add .`.
14. Bei Befehlen immer vorher sagen:

    * wo ausführen
    * was der Befehl macht
    * wann stoppen
    * welche Ausgabe ich schicken soll

15. Maximal ein Befehlsblock pro Antwort.
16. Keine PowerShell-, Server- und DB-Schritte in einem Schritt mischen.
17. Keine RDAP-Arbeitsordner/Deploy-Clones/Backups in `/root`.

Server-Regeln:

* Deploy-/Test-Clones: `/opt/stream-control-center/_deploy_tmp/`
* Runtime-/Temp: `/opt/stream-control-center/_runtime_tmp/`
* Backups: `/var/backups/stream-control-center/`
* Keine neuen RDAP-Arbeitsordner, Deploy-Clones, Temp-Dateien oder Backups nach `/root`.

Wichtige Webserver-/DB-Fakten:

* Webserver: `web.cgn.community`
* Public Subdomain: `mods.forrestcgn.de`
* Service: `scc-remote-modboard.service`
* Intern: `127.0.0.1:3010`
* DB-Typ: MariaDB 11.8.6
* DB_NAME: `c3stream_control`
* DB_USER: `c1stream_control`
* Passwort niemals dokumentieren oder ausgeben.

Wichtige aktuelle Dateien:

* `remote-modboard/backend/package.json`
* `remote-modboard/backend/server.js`
* `remote-modboard/backend/.env.example`
* `remote-modboard/backend/README.md`
* `remote-modboard/backend/src/app.js`
* `remote-modboard/backend/src/routes/health.routes.js`
* `remote-modboard/backend/src/routes/status.routes.js`
* `remote-modboard/backend/src/routes/routes.routes.js`
* `remote-modboard/backend/src/routes/auth-model.routes.js`
* `remote-modboard/backend/src/routes/auth-status.routes.js`
* `remote-modboard/backend/src/routes/auth-twitch.routes.js`
* `remote-modboard/backend/src/services/config.service.js`
* `remote-modboard/backend/src/services/db-health.service.js`
* `remote-modboard/backend/src/services/db.service.js`
* `remote-modboard/backend/src/services/auth-db-read.service.js`
* `remote-modboard/backend/src/services/auth-session-read.service.js`
* `remote-modboard/backend/src/services/auth-status.service.js`
* `remote-modboard/backend/src/security/safety.js`

Beginne im neuen Chat bitte mit:

1. Dateien lesen.
2. Kurz bestätigen, welchen Stand du gefunden hast.
3. Scope für `RDAP8_PERMISSION_CHECK_MIDDLEWARE_PLAN` nennen.
4. Dann auf mein `go` warten.
