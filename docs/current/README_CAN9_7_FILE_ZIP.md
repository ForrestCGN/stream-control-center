# CAN-9.7 ZIP

Entpacken nach:

```text
D:\Git\stream-control-center
```

Danach ausführen:

```cmd
node -c htdocs\dashboard\modules\bus_diagnostics.js
.\stepdone.cmd "CAN-9.7 Recovery-Preflight Dashboard Route Consumption umgesetzt"
```

Falls das Dashboard alte Daten zeigt: Browser hart neu laden und Backend neu starten, falls nötig.
