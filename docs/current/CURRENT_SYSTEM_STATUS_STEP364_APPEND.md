# CURRENT_SYSTEM_STATUS STEP364 Append

STEP364 ergänzt nach STEP360/362/363 einen reinen Diagnose-Watcher für den Alert-Lifecycle.

Geprüft wird, ob laufende Alerts nach Ende sauber aus `currentEventId` / `currentStatus` verschwinden.

Keine Codeänderung.
