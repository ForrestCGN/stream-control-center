# CURRENT CHAT HANDOFF – LWG-4N.6e

Bestätigter UX-Bedarf: Der Glücksrad-Editor soll keinen sichtbaren `aktiv`-Haken mehr pro Feld zeigen. Wenn ein Feld im Editor vorhanden ist, ist es gewollt und wird verwendet. Wenn es nicht gewollt ist, wird es entfernt.

Geändert wurde nur `htdocs/dashboard/modules/loyalty_games.js`.

Keine Backend-/API-/DB-Änderungen.
