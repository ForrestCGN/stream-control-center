# CAN-9.8 ZIP

Entpacken nach:

```text
D:\Git\stream-control-center
```

Danach ausführen:

```cmd
.\stepdone.cmd "CAN-9.8 Recovery-Preflight Dashboard Route Consumption Live-Test abgenommen"
```

`node -c` ist nicht nötig, weil keine JS-Datei enthalten ist.
