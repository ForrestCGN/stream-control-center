# START HERE FOR NEW CHAT

Aktueller Stand: `0.2.10H - Dashboard-v2 Remote-Asset-Pfade lokal repariert`.

Verbindlich:

```text
Lokales Dashboard-v2 soll optisch und strukturell eine exakte Kopie des Remote-Modboards sein.
Nicht lokal nachbauen, sondern Remote-Modboard-Shell/Assets als Wahrheit verwenden.
Nur Datenquelle und Sicherheitsgrenzen duerfen lokal abweichen.
```

0.2.10H repariert den Fehler aus 0.2.10G, bei dem CSS/JS nicht geladen wurden. Der lokale Adapter liefert oder leitet Remote-Modboard-Assets fuer `/assets/...` und `/dashboard-v2/assets/...` weiter.

Weiterarbeit: Erst Sichttest, dann ggf. stepdone. Keine fachliche Erweiterung vor erfolgreichem Sichttest.
