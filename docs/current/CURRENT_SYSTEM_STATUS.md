STEP274D aktiv vorbereitet: Die zentrale Medienverwaltung besitzt jetzt eine Resolver-Schicht für Module. Assets können über `/api/media/resolve` oder intern über `media.resolveAssetForUse(...)` einheitlich aufgelöst werden. Commands nutzen diese Resolver-Logik für Sound-/Video-Auswahloptionen. Die tatsächliche Sound-/Video-Ausführung wurde bewusst noch nicht aktiviert, weil `sound_system` dafür im nächsten Step sauber media-registry-aware gemacht werden muss.
