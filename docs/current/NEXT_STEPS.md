# Next Steps

1. LWG-4N.7 einspielen und Runtime testen.
2. Prüfen, ob neue Giveaway-Glücksräder ohne Source-Preset vollständig funktionieren.
3. Danach UI/Detail-Status für „Rad-Drehung offen“ und Auslosen weiter verfeinern.

## AutoShout / Shoutout – nächste sinnvolle Schritte

1. Test-Account `forrestcgn` wieder aus der AutoShout-Streamer-Liste entfernen, falls er nur für den Test eingetragen wurde.
2. `papselzockt_` vs. `papselzockt_cgn` prüfen und den korrekten Twitch-Login in der AutoShout-Liste hinterlegen.
3. Später optional Diagnose verbessern: `lastSeenLogin`, `lastSeenMessage`, `lastDecision`, `instantTrigger`, `matchedStreamer`, `blockedByStreamDayLimit`.
4. Keine weitere AutoShout-Codeänderung ohne neuen Auftrag.
