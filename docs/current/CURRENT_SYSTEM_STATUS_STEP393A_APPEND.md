# CURRENT SYSTEM STATUS – STEP393A Append

STEP393A korrigiert ausschließlich das Quick-Status-Diagnoseskript für Windows PowerShell 5.1.

Der stabile Stand aus STEP393 bleibt gültig:

- direktes `_overlay-alerts-v2.html` ist die aktive Produktions-Alertquelle
- `alert_overlay_v2_shadow` ist der direkte Bus-Client im echten Overlay
- iframe-Bridge wird nicht produktiv verwendet
- Reconnect/Refresh wurde vom Nutzer als funktionierend bestätigt

Keine Runtime-Dateien wurden geändert.
