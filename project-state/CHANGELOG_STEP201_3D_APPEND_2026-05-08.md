# CHANGELOG Ergänzung – STEP201.3d

Stand: 2026-05-08

## Backend

- `GET /api/messages/routes` vorbereitet.
- `GET /api/messages/integration-check` vorbereitet.
- `GET /api/messages/config` als read-only Standard-Alias vorbereitet.
- `GET /api/messages/settings` als read-only Standard-Alias vorbereitet.
- Testscript mit Logging nach `D:\gpt\last_api.json` ergänzt.

## No behavior changes

Keine Änderung an:

```text
Message-Rendering
Message-Send
Discord-Bridge
Scheduler-Start/Stop
DB-Schema
JSON
Dashboard
bestehenden Routen
```
