# CHANGELOG Append – STEP201.10c Scene-Control

## 2026-05-08

- Added Scene-Control diagnostic endpoints under `/api/scene`.
- Added non-destructive `POST /api/scene/reload`.
- Kept existing `/api/scene/health`, `/api/scene/list` and `/api/scene/set` behavior unchanged.
- Did not add `/api/scene-control`, `/api/scene_control` or `/api/scenes` aliases.
