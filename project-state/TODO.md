# TODO

Stand: 2026-06-27

- Cleanup 7 lokal einspielen und Dry-Run pruefen.
- Mit `-Execute` nur die 1033 exakt dokumentierten `ARCHIVE_OR_MERGE`-Dateien nach `docs/archive/docs-current-cleanup-7/` verschieben.
- Keine Deletes in Cleanup 7.
- `KEEP_CURRENT` und `REVIEW_MANUALLY` nicht anfassen.
- Optional erzeugte `_handoff`-Reports bewusst committen oder lokal loeschen, nicht untracked liegen lassen.
- Danach Cleanup 8 vorbereiten: die 40 `REVIEW_MANUALLY`-Dateien fachlich lesen und einzeln entscheiden.
- Technischer Basisstand 0.1.3 bleibt unveraendert: read-only, keine Writes.
