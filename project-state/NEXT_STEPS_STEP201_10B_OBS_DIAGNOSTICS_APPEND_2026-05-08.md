# NEXT_STEPS Append – nach STEP201.10b

Nach erfolgreichem Live-Test ist OBS im STEP201-Diagnose-Standard.

Nächster sinnvoller Schritt:

```text
STEP201.10c – Scene-Control separat bewerten
```

Dabei soll der produktive Prefix `/api/scene` berücksichtigt werden.
Keinen neuen Prefix `/api/scene-control` erfinden, ohne fachlichen Grund.
