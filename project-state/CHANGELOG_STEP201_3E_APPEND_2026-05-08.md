# CHANGELOG Ergänzung – STEP201.3e

Stand: 2026-05-08

## Backend

- `GET /api/message-rotator/routes` vorbereitet.
- `GET /api/message-rotator/integration-check` vorbereitet.
- `GET /api/message-rotator/config` als read-only Standard-Alias vorbereitet.
- `GET /api/message-rotator/settings` als read-only Standard-Alias vorbereitet.
- Testscript mit Logging nach `D:\gpt\last_api.json` ergänzt.

## No behavior changes

Keine Änderung an:

```text
Rotator-Auswahl
Chat-Ticks
Next/Manual/Send
Live-Status
Start/Stop
DB-Schema
JSON
Dashboard
bestehenden Routen
```
