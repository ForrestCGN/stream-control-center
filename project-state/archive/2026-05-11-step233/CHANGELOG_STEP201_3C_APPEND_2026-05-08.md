# CHANGELOG Ergänzung – STEP201.3c

Stand: 2026-05-08

## Backend

- `GET /api/todo/routes` vorbereitet.
- `GET /api/todo/integration-check` vorbereitet.
- `GET /api/todo/config` als read-only Standard-Alias vorbereitet.
- `GET /api/todo/settings` als read-only Standard-Alias vorbereitet.
- Testscript mit Logging nach `D:\gpt\last_api.json` ergänzt.

## No behavior changes

Keine Änderung an:

```text
Todo-Einträgen
Discord-Post
Stats
DB-Schema
JSON
Dashboard
bestehenden Routen
```
