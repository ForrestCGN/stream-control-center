# CHANGELOG Ergänzung – STEP201.3b

Stand: 2026-05-08

## Backend

- `GET /api/tagebuch/routes` vorbereitet.
- `GET /api/tagebuch/integration-check` vorbereitet.
- `GET /api/tagebuch/config` als read-only Standard-Alias vorbereitet.
- `GET /api/tagebuch/settings` als read-only Standard-Alias vorbereitet.
- Testscript mit Logging nach `D:\gpt\last_api.json` ergänzt.

## No behavior changes

Keine Änderung an:

```text
Tagebuch-Einträgen
Streamstart/Streamende
Discord-Webhook
Stats
Reset
DB-Schema
JSON
Dashboard
bestehenden Routen
```
