
## STEP201.7 - Clip Diagnose-Endpunkte

Clip wurde auf den STEP201-Diagnose-Standard vorbereitet. Produktiver Prefix bleibt `/api/clip`; `/api/clips` wird bewusst nicht registriert.

Neue Endpunkte: `/api/clip/config`, `/api/clip/settings`, `/api/clip/routes`, `/api/clip/integration-check`, `POST /api/clip/reload`.
