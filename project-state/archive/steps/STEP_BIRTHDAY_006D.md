# STEP_BIRTHDAY_006D - Overlay Text Polish

## Änderungen
- Birthday-Overlay zeigt in der Headline immer `Happy Birthday!`.
- Alter bleibt im Glückwunschtext/Subline erhalten, wenn vorhanden.
- Sichtbares Status-Badge `Party läuft` wurde ausgeblendet.
- Backend erzeugt für Show-Headlines kein `Happy 51. Birthday!` mehr.
- Overlay hat zusätzlich einen Safety-Cleaner für alte States mit Alters-Headline.

## Nicht geändert
- Keine Command-/Queue-/Sound-System-Logik.
- Keine Upload-/Dashboard-Logik.
