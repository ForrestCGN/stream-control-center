# STEP201.10c Append – Scene-Control Diagnose-Endpunkte

Scene-Control wurde unter dem bestehenden Prefix `/api/scene` um Diagnose-Endpunkte ergänzt.

Ergänzt:

```text
/status
/config
/settings
/routes
/integration-check
/reload
```

Bestehende Routen `/api/scene/health`, `/api/scene/list` und `/api/scene/set` bleiben erhalten.
