# CHANGELOG

## CAN-42.14c - Dashboard Diagnostics Label-Feinschliff

Geändert:

- `htdocs/dashboard/modules/diagnostics_generic_details.js`
  - `MODULE_VERSION` auf `0.1.3-can42-14c` erhöht.
  - Datenbank-Labels weiter geglättet: `Datenbank-Typ`, `Datenbank-Pfad`, `Datenbank-Schema`, `Erwartetes Schema`, `Datenbank-Fehler`.
  - Statusfeld `Phase` wird als `Statusphase` angezeigt.
  - Abschnittsüberschriften `Queue` und `Runtime` werden als `Warteschlange` und `Laufzeit` angezeigt.

Nicht geändert:

- kein Backend
- keine Statusrouten
- keine DB
- keine produktiven Aktionen
- keine neue Moduldatei
- keine Funktionalität entfernt
