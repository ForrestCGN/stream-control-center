# CHANGELOG – STEP222b / LWG-6.3b

- Testscript akzeptiert zusammengefasste Command-Bridge-Antworten.
- Prozent-Einsatz wird zusätzlich über den neuen Chattext validiert, wenn `bet` nicht strukturiert sichtbar ist.
- `messageKey` wird nur geprüft, wenn er in der Antwort vorhanden ist.
- Keine Runtime-Codeänderung gegenüber STEP222a.
