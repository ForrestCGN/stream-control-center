# CHANGELOG

## STEP274C
- Backend-Brücke `commands_media.js` ergänzt.
- `/api/commands/media-options` für Command-Dashboard-Medienauswahl ergänzt.
- `/api/commands/media-bridge/status` ergänzt.
- Dashboard-Brücke `commands_media.js` ergänzt.
- Command-Editor ersetzt Sound-/Video-Medium-ID-Felder durch Medien-Dropdowns aus `media_assets`.
- Bestehende Command-Ausführung nicht verändert.

## STEP274B
- Media-Core auf STEP274B gehoben.
- Typ-Erkennung für überlappende Endungen wie `.webm` und `.gif` repariert.
- Pfadprüfungen für Scan/Delete gehärtet.
