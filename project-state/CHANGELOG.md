# CHANGELOG – STEP438

- Added `GET /api/vip-sound/sounds/files` to list existing VIP sound files for diagnostics.
- Added admin-test support for `useExistingSound=true` and `testSoundFile`.
- Added `adminTestSoundOverride` diagnostics to the real-flow Guard snapshot.
- Kept the normal Twitch command role guard unchanged.
- Kept productive VIP delivery on `legacy_sound_system_api`.
