# CAN44.42 Dashboard Effective Stream State Display Status

## Status
Gebaut.

## Änderung
Dashboard-Anzeige trennt jetzt effektiven Stream-State von echten Quellen.

## Zielprüfung
Bei aktivem `manual_override` soll der Hero `ONLINE (Override)` beziehungsweise `OFFLINE (Override)` anzeigen. Rechts bleiben die echten Quellen sichtbar.

## StepDone
Nach Entpacken ausführen:

```cmd
.\stepdone.cmd "CAN44.42 Dashboard Effective Stream State Display"
```
