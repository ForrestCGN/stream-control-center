# CURRENT STATUS – STEP433

VIP Bus mode runtime status is fixed. Runtime mode changes through `/eventbus/sound-command/mode` remain visible in `/eventbus/sound-command/status` until reset or server restart.

Productive VIP flow still remains legacy `/api/sound/play`.
