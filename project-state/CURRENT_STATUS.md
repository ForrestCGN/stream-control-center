Aktueller Stand: STEP274D – zentrale Media-Resolver-Schicht ergänzt. Medienverwaltung kann Assets jetzt über `/api/media/resolve` und `media.resolveAssetForUse(...)` für Module auflösen. Commands nutzen für Medienoptionen dieselbe zentrale Resolver-Logik. Die echte Sound-/Video-Ausführung bleibt bewusst getrennt und folgt im nächsten Step, damit `sound_system` sauber media-registry-aware gemacht wird.
