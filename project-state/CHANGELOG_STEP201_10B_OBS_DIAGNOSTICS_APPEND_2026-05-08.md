# CHANGELOG Append – STEP201.10b OBS

## 2026-05-08

- Added OBS diagnostic endpoints under `/api/obs`.
- Added non-destructive `POST /api/obs/reload`.
- Kept existing OBS runtime, scene, replay, audio, filter and media routes unchanged.
- Kept legacy `/obs/...` routes unchanged.
